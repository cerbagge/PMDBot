# Planet Earth 디스코드 봇 가이드

마인크래프트 Planet Earth 서버와 디스코드를 연동하는 봇입니다.
플레이어 인증, 역할 자동 부여, 여행 시스템 등을 제공합니다.

---

## 일반 사용자 명령어

### `/확인` - 국가 확인 및 역할 부여
마인크래프트 계정을 확인하고 국가/마을에 맞는 역할을 자동으로 받습니다.

- 디스코드에 연결된 마인크래프트 계정 확인
- 소속 국가와 마을 표시
- 해당하는 역할 자동 부여

---

### `/콜사인` - 개인 콜사인 설정
개인 콜사인(별명)을 설정합니다. **15일 쿨타임**이 있습니다.

| 매개변수 | 설명 |
|---------|------|
| `텍스트` | 콜사인 (최대 20자) |

**제한사항:**
- 본국 국민만 사용 가능
- 콜사인 금지된 사용자는 사용 불가
- 15일마다 1회 변경 가능

---

### `/국가설정` - 개인 국가 설정
시스템에 자신의 국가를 수동으로 설정합니다.

| 매개변수 | 설명 |
|---------|------|
| `국가` | 국가명 (API 영문 이름과 일치해야 함) |

---

### `/도움말` - 명령어 도움말
모든 사용 가능한 명령어와 봇 상태를 확인합니다.

- 사용자/관리자 명령어 목록
- API 연결 상태
- 동맹 수, 대기열 상태

---

### `/여행신청` - 여행 신청
임시로 국가를 떠나는 여행을 신청합니다.

**입력 항목:**
- 목적지 국가
- 출발일 (YYYY.MM.DD 형식)
- 종료일 (YYYY.MM.DD 형식)

**참고:** 관리자 승인이 필요합니다.

---

## 관리자 명령어

### 기본 관리

| 명령어 | 설명 |
|--------|------|
| `/테스트` | 봇 기능 테스트 |
| `/스케줄확인` | 자동 실행 스케줄 확인 |
| `/로그확인` | 봇 운영 로그 확인 |
| `/로그관리` | 로그 정리/내보내기 |
| `/데이터베이스` | 데이터베이스 관리 |

---

### 대기열 관리

#### `/대기열상태`
현재 대기열 상태를 확인합니다.

#### `/대기열추가`
사용자 또는 역할 멤버를 대기열에 추가합니다.

| 매개변수 | 설명 |
|---------|------|
| `선택` | "유저" 또는 "역할" |
| `유저` | 디스코드 멤버 (유저 선택 시) |
| `역할` | 디스코드 역할 (역할 선택 시) |

#### `/대기열초기화`
대기열을 완전히 비웁니다.

#### `/자동실행`
자동 실행할 역할을 추가합니다.

| 매개변수 | 설명 |
|---------|------|
| `역할id` | 역할 ID (숫자) |

#### `/자동실행시작`
수동으로 자동 역할 부여를 시작합니다.

---

### 콜사인 관리 (`/콜사인관리`)

| 기능 | 설명 |
|------|------|
| `사용자_조회` | 사용자의 콜사인 및 금지 상태 조회 |
| `콜사인_변경` | 사용자 콜사인 강제 변경 (쿨타임 무시) |
| `전체_목록` | 모든 콜사인 목록 (페이지당 20개) |
| `권한박탈` | 사용자 콜사인 사용 금지 |
| `권한복구` | 콜사인 사용 금지 해제 |
| `권한박탈_목록` | 금지된 사용자 목록 |
| `쿨타임_초기화` | 특정/전체 사용자 쿨타임 초기화 |
| `데이터_백업` | 콜사인 데이터 수동 백업 |
| `백업_목록` | 백업 파일 목록 (최근 15개) |
| `데이터_복구` | 백업에서 복구 |
| `역할_양식` | 역할별 닉네임 형식 설정 |
| `역할_양식_목록` | 역할 양식 설정 목록 |
| `역할_양식_제거` | 역할 양식 제거 |
| `백업파일_업로드` | 백업 파일 업로드 및 복구 |

**닉네임 양식 변수:**
- `{MC}` - 마인크래프트 닉네임
- `{NN}` - 디스코드 닉네임
- `{TT}` - 마을명
- `{CC}` - 콜사인

---

### 예외 관리 (`/예외설정`)

| 기능 | 설명 |
|------|------|
| `추가` | 자동 처리 제외 사용자 추가 |
| `제거` | 예외 목록에서 제거 |
| `목록` | 예외 사용자 목록 확인 |

---

### 동맹 관리

#### `/동맹설정`

| 기능 | 설명 |
|------|------|
| `추가` | 동맹 국가/마을 추가 |
| `제거` | 동맹 제거 |
| `목록` | 동맹 목록 확인 |
| `역할설정` | 동맹에 역할 할당 |

#### `/동맹확인`
모든 멤버의 동맹 역할을 검증하고 업데이트합니다.

---

### 마을 역할 관리

#### `/마을역할`

| 기능 | 설명 |
|------|------|
| `추가` | 마을-역할 연결 생성 |
| `제거` | 마을-역할 연결 제거 |
| `목록` | 마을-역할 매핑 확인 |
| `마을목록` | 마을 식별자 찾기 가이드 |

#### `/마을테스트`
마을 역할 시스템 테스트

---

### 여행 관리 (`/여행관리`)
여행 신청을 승인/거부하고 기록을 관리합니다.

---

## 자동화 시스템

### 1. 자동 실행 스케줄러
- **기본:** 매주 수요일 03:24 UTC
- 대기열의 사용자 자동 처리
- 국가/마을 기반 역할 부여

### 2. 대량 데이터 자동 업데이트
- **주기:** 15분마다
- 마인크래프트 닉네임 변경 시 자동 동기화
- 국가 변경 시 역할 자동 업데이트

### 3. 여행 스케줄러
- **주기:** 1시간마다
- 여행 리마인더 발송 (출발 1일 전, 당일)
- 만료된 여행 자동 완료 처리

### 4. 콜사인 백업 스케줄러
- **자동 백업:** 매주 월요일 08:00
- **보관 기간:** 30일
- **저장 위치:** `callsign_backups/`

---

## 권한 레벨

| 레벨 | 사용 가능 명령어 |
|------|-----------------|
| **일반 사용자** | `/확인`, `/콜사인`, `/국가설정`, `/도움말`, `/여행신청` |
| **관리자** | 모든 관리자 명령어 (Administrator 권한 필요) |
| **서버 소유자** | `/국가설정` 등 특수 명령어 |

---

## 주요 기능

- **자동 멤버 추가** - 새 멤버 가입 시 자동으로 대기열에 추가
- **예외 시스템** - 특정 사용자 자동 처리 제외
- **쿨타임 관리** - 콜사인 15일 쿨타임 (관리자 우회 가능)
- **UUID 기반 추적** - API 변경에도 정확한 동맹 추적
- **템플릿 형식** - 역할별 닉네임 템플릿 지원
- **지속형 뷰** - 봇 재시작 후에도 버튼 동작
- **종합 로깅** - 모든 관리자 작업 로그 채널에 기록

---

## 환경 설정

주요 환경 변수:
- `DISCORD_TOKEN` - 봇 인증 토큰
- `GUILD_ID` - 대상 서버 ID
- `BASE_NATION` - 본국 (기본값: Red_Mafia)
- `MC_API_BASE` - Planet Earth API URL
- `SUCCESS_CHANNEL_ID` - 성공 알림 채널
- `FAILURE_CHANNEL_ID` - 실패 알림 채널
- `LOG_CHANNEL_ID` - 관리자 로그 채널
